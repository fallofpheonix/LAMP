[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
  "task1-path-tracing",
  "task2-viewsheds",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]
